export interface Bookmarked {
    novelId: string,
    chapterId: string
    name: string
    title: string,
    author: string
    year: number,
    views: number,
    rating: number,
    imagesURL: string,
    genreName: [
    ],
    genreIds: [
    ],
    description: string,
    status: boolean,
    approvalStatus: boolean,
    numChapter: number
}