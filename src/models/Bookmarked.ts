export interface Bookmarked {
    novelId: string,
    accountId:string,
    chapterId: string,
    chapterIndex: number,
    name: string
    title: string,
    author: string
    year: number,
    views: number,
    rating: number,
    imagesURL: string,
    genreName: [
    ],
    genreIds: [
    ],
    description: string,
    status: boolean,
    approvalStatus: boolean,
    numChapter: number
}